# Project media

```
img/
  services/          # all job photos/videos by service type
  before-after/      # curated Before/After pairs for the website
```

- **132** files organized under `services/` (every WhatsApp upload).
- The site uses a curated subset on Home + Projects; `before-after/` holds 3 clear pairs.
- Empty service folders keep a `.gitkeep` so the structure stays in git until media is added.
